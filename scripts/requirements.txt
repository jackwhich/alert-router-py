fastapi==0.110.0
uvicorn==0.27.1
requests>=2.32.2  # 更新以满足 opensearch-py 和 datasets 的要求
requests[socks]>=2.32.2  # 支持 SOCKS 代理（可选，如果不需要 SOCKS 代理可以删除 [socks]）
PyYAML==6.0.1
Jinja2==3.1.3
matplotlib==3.9.2
plotly==5.22.0  # 用于生成更美观的图表
kaleido==0.2.1  # Plotly 静态图片导出引擎
numpy==1.26.4  # 数据处理（如果还没有）
python-dateutil==2.9.0  # 日期解析（如果还没有）
python-dotenv>=1.0.1  # 更新以满足 pymilvus 的要求
